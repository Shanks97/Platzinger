import { GetFriendsOffPipe } from './get-friends-off.pipe';

describe('GetFriendsOffPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFriendsOffPipe();
    expect(pipe).toBeTruthy();
  });
});

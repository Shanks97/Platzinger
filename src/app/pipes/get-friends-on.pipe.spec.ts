import { GetFriendsOnPipe } from './get-friends-on.pipe';

describe('GetFriendsOnPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFriendsOnPipe();
    expect(pipe).toBeTruthy();
  });
});
